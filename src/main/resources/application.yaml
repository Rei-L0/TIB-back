spring:
  application:
    name: tib

  profiles:
    active: dev

weather:
  api:
    key: ${WEATHER_API_KEY}

deepl:
  api-key: ${DEEPL_API_KEY}

cdn:
  url: ${SHORTS_CDN_URL:https://d2n4hxvymniuy1.cloudfront.net}
